# ─────────────────────────────────────────────
# Defect Detection — Configuration File
# ─────────────────────────────────────────────

[dataset]
n_per_class  = 200
image_size   = 64
noise_level  = 0.08
random_seed  = 42

[model]
n_estimators      = 100
max_depth         = 15
min_samples_split = 5
test_size         = 0.2

[output]
results_filename = defect_detection_results.png
dpi              = 150
